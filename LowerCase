#include <stdio.h>
void main()
{
    char a[100];
    gets(a);
    int i=0;
    int x=0;
    for(i=0;x==0;i++)
    {
        if(a[i]==0)
        {
            x=1;
        }
        if(a[i]<90&&x==0)
        {
            a[i]=a[i]+32;
        }
    }
    puts(a);
}
